# Minor

#### Feature

- Allow dynamic site finding for sprites - uses `ViteEntrypoints.json`
- Add `@` alias for `app/sites`

#### Fix

- Process SVGs with decimal pixels
- Allow CSS imports accross local files
- Replace deprecated includePaths

#### Refactor

- Extract sprite functionality to it's own module